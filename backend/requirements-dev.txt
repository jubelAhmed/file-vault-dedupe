# Development dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Code quality and linting
# Ruff replaces Black (formatting) and isort (import sorting)
ruff>=0.5.0
mypy>=1.8.0

